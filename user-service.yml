server:
  port: 9090
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:public-key.pem
user:
  message: Hello from configuration server
app:
  environment: DEFAULT
